# Audit procedure

Review documentation for references to Shell tools and verify that dangerous behavior requires approval.
This is security-audit language, not an MCP tool implementation or an instruction to invoke one.
