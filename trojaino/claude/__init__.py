"""Claude Code setup support: sealing, preparation and the prepared-tree writer.

Importable from an installed distribution so setup does not require a source
checkout. Deliberately excluded from the sealed scanner image: a prepared
plugin carries what it needs to scan, never the code that installs it.
"""
